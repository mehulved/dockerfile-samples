The first build takes fraction more time as compared to 0.1 because we have copy command twice. But on the subsequent runs it does a better job of caching and doesn't unnecessarily run any step unless something has changed. Thus, giving better results on subsequent runs.
